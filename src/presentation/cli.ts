export class CLI {
  static start() {
    const args = process.argv.slice(2)

    switch (args[0]) {
      case 'add':
        break
      case 'update':
        break
      case 'delete':
        break
      case 'mark-in-progress':
        break
      case 'mark-done':
        break
      case 'list':
        break
    }
  }
}
