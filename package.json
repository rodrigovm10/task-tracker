{
  "name": "task-traker-cli",
  "version": "1.0.0",
  "description": "Task traker is a project used to track and manage your tasks",
  "main": "app.js",
  "bin": {
    "task-cli": "./dist/app.js"
  },
  "scripts": {
    "dev": "tsnd --respawn --clear src/app.ts",
    "build": "rimraf ./dist && tsc",
    "start": "npm run build && node dist/app.js"
  },
  "keywords": [],
  "author": "Rodrigo Vega",
  "license": "ISC",
  "packageManager": "pnpm@10.5.2",
  "devDependencies": {
    "@types/node": "^22.13.11",
    "rimraf": "^6.0.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.8.2"
  }
}
